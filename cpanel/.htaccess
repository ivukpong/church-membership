<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

# If request is not a real file or directory, serve SPA entry

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L]
</IfModule>
